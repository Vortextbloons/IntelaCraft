import assert from "node:assert/strict";import {createServer} from "node:http";import {after,describe,it} from "node:test";import {discoverModels,planRequest,testProvider,type ProviderProfile} from "./index.js";
const server=createServer((req,res)=>{res.setHeader("Content-Type","application/json");if(req.url==="/v1/models")res.end(JSON.stringify({data:[{id:"test-model"}]}));else res.end(JSON.stringify({choices:[{message:{content:JSON.stringify({summary:"plan",inspection:[{toolName:"inspect.players",arguments:{},summary:"players"}],actions:[],verification:[],notes:[]})}}]}));});await new Promise<void>(r=>server.listen(0,r));const a=server.address();assert.ok(a&&typeof a==="object");const p:ProviderProfile={id:"p",name:"P",baseUrl:`http://127.0.0.1:${a.port}/v1`,apiKey:"secret",model:"test-model"};after(()=>server.close());
describe("OpenAI-compatible Pi runtime",()=>{it("discovers and tests models",async()=>{assert.deepEqual(await discoverModels(p),["test-model"]);assert.equal((await testProvider(p)).ok,true);});it("parses a structured plan",async()=>{const plan=await planRequest(p,"who is online",{});assert.equal(plan.inspection[0].toolName,"inspect.players");});});
