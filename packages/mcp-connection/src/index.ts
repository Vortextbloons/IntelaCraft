export class AdvisoryMcpClient {constructor(private url?:string,private token?:string){} status(){return{configured:Boolean(this.url),available:Boolean(this.url),advisoryOnly:true};}async query(question:string):Promise<unknown>{if(!this.url)throw new Error("MCP is not configured");const headers:Record<string,string>={"Content-Type":"application/json"};if(this.token)headers.Authorization=`Bearer ${this.token}`;const r=await fetch(this.url,{method:"POST",headers,body:JSON.stringify({jsonrpc:"2.0",id:Date.now(),method:"tools/call",params:{name:"search",arguments:{query:question}}}),signal:AbortSignal.timeout(15000)});if(!r.ok)throw new Error(`MCP ${r.status}`);return r.json();}}
